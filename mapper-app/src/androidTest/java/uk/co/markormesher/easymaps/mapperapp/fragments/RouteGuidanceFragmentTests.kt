package uk.co.markormesher.easymaps.mapperapp.fragments

class RouteChooserFragmentTests {

	/*
	MANUAL TESTS

	Given: the route chooser fragment is active
	When: two locations are entered
	Then: the search results should update

	Given: the route chooser fragment is active
	When: the same location is entered twice
	Then: a "you're already there" message should be displayed

	Given: the route chooser fragment is active
	Given: two locations have been selected
	When: the "swap locations" icon is selected
	Then: the locations should swap and the search results should update

	Given: the route chooser fragment is active
	When: a route option is selected
	Then: the route guidance fragment should be activated
	 */

}

package uk.co.markormesher.easymaps.scannerapp.activities

class EntryActivityTests {

	/*
	MANUAL TESTS

	Given: the app has been reinstalled
	When: the app is opened for the first time
	Then: the entry activity should prompt for permissions

	Given: the app has been installed already
	When: the app is opened again
	Then: the entry activity should not prompt for permissions

	Given: the entry activity has prompted for permissions
	When: the user grants permissions
	Then: the entry activity should launch the main activity

	Given: the entry activity has prompted for permissions
	When: the user denys permissions
	Then: the entry activity should retry the permissions prompt
	 */

}
